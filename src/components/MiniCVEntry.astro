---
interface Props {
  year: string;
  title: string;
  institution: string;
  location: string;
  description: string;
}

const { year, title, institution, location, description } = Astro.props;
---

<div class="flex flex-col space-y-2 md:space-y-0 md:flex-row md:justify-between md:items-center">
    <span>{title}</span>
    <div class="font-bold text-lg md:text-xl w-full md:w-auto">{year}</div>
  
    <div class="flex flex-col w-full md:w-auto md:flex-row md:space-x-2 text-center md:text-left">
        <span class="hidden md:inline">—</span>
        <span>{institution}, {location}</span>
    </div>
</div>

<p class="text-base">{description}</p>
